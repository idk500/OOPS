# OOPS 产品规格 (SPEC) — 唯一事实来源

> 本文件是 OOPS 的产品规格。一切功能开发以本文件为准;偏离本文件的既有代码应被收束/裁剪。
> 最后更新:2026-08-12

---

## 1. 一句话定位

OOPS 是**绝区零一条龙(OneDragon)的「兜底恢复工具」**:当用户**无法完成一条龙的安装 / 启动 / 更新**时,双击本工具——它能尽量把项目**恢复到最新可用状态并启动**;恢复不了时,把问题**明明白白显示在屏幕上(可拍屏)**,让远程支持能定位。

- ❌ 它**不是**"运行前体检"工具(旧定位,已废弃)。
- ❌ 它**不是**通用系统检测器。
- ✅ 它是"**卡住了来救我**"的工具。

## 2. 用户画像(决定一切设计)

- 完全零计算机基础;**不会**装 python/git/uv,**不会**改环境变量/字符集,**不会**用命令行,**不会**在 issue 贴日志。
- **只会**:下载 zip → 解压 → 双击 exe → 用手机拍屏幕发给客服。
- 推论:零依赖、一次双击、出错**必须屏幕可见**(绝不闪退 / 绝不静默失败)。

## 3. 设计原则(硬约束)

1. **零依赖**:不要求用户装 python/git/uv,不改字符/环境变量。exe 自带 Python(PyInstaller);无 git 自动下 MinGit;都做不到就明确报错。
2. **不闪退 / 不静默**:任何错误 → 置顶弹窗(tkinter)+ 写 `oops-error.txt`,绝不闪黑框退出。
3. **一次双击全自动**:不向用户提问、不让选择。倒数仅作可见提示,到点自动继续。
4. **能恢复就恢复,不能就讲清**:网络通则**绕过一条龙原更新链路**(它可能就是坏的)从 CNB 强更 + 启动;不通则弹窗说"卡在 X,怎么办"。
5. **CNB 优先**:国内默认走 CNB 镜像,GitHub / Gitee 兜底。
6. **诊断驱动**:先快速探硬阻塞(系统 / 网络 / git / 磁盘 / 路径),再决定恢复 or 弹窗,**而不是闷头试到崩**。
7. **错误对远程可读**:弹窗文案 + `oops-error.txt` 要让客服凭一张照片就能定位(含:发生了什么、在哪个环节、可能的根因、用户的系统/网络/路径概况)。

## 4. 主流程(双击 oops.exe,放一条龙根目录)

```
[置顶小窗显示进度,出错留窗]
1. 快速诊断(秒级,结构化结果)
   • 系统:版本(≥Win10)、架构(64位)、时间(影响 TLS)
   • 磁盘/路径:可用空间、中文/特殊字符、长度、写权限、是否本地盘
   • git:在不在(不在→自动 MinGit;下不来→弹窗)
   • 网络:github / cnb / gitee 三源各探一下,记录谁通谁不通
   • 项目:是否一条龙根目录、是否 git 仓、是否落后/分叉、launcher 在不在
2. 决策
   • 硬阻塞(系统过低 / 完全断网 / 磁盘满 / 路径非法 / 无写权限)
       → 置顶弹窗:明确原因 + 下一步建议
   • 至少 CNB(或任一源)通
       → 绕过原路径,用专用 remote 从该源强更到最新 → 启动 launcher
   • 三源全不可达
       → 弹窗:检查网络 / 代理 / 防火墙
3. 任何异常 → 置顶弹窗 + oops-error.txt(可拍屏)
```

## 5. 场景矩阵(需逐条覆盖:检测方法 + 能否自动恢复 + 给用户的中文提示)

### 5.1 系统环境
| 场景 | 检测 | 处置 | 用户提示(示例) |
|---|---|---|---|
| WinXP/7/8 | `sys.getwindowsversion` / platform | 不可恢复 | "系统版本过低,需要 Windows 10 64 位及以上" |
| 32 位系统 | `platform.machine`/架构 | 不可恢复 | "需要 64 位 Windows" |
| 系统时间错误 | TLS 证书校验失败/时间偏差 | 提示 | "系统时间不准,导致证书校验失败,请校准时间后重试" |
| 缺 VC 运行库 | 启动报缺 VCRUNTIME/DLL | 提示 | "缺少 Visual C++ 运行库,请安装 VC Redist(链接)" |
| 杀软误杀/隔离 | exe 不运行/文件消失 | 文档 | "杀毒软件可能拦截了 oops.exe,请加白名单" |
| SmartScreen 拦截 | 用户侧现象 | 文档(README 截图) | "点击‘更多信息’→‘仍要运行’" |

### 5.2 网络
| 场景 | 检测 | 处置 | 用户提示 |
|---|---|---|---|
| 完全断网 | 连 cnb.cool/gitee 失败 + DNS 失败 | 弹窗 | "无法联网,请检查网络连接(WiFi/网线/路由)" |
| DNS 损坏 | 域名解析失败 | 弹窗 | "无法解析域名,请检查 DNS(可改 223.5.5.5 / 114.114.114.114)" |
| GitHub 不通(国内常态) | 测 github.com | **自动**转 CNB/Gitee | (静默切换,不打扰) |
| CNB 不通 | 测 cnb.cool | 转其他源 | (静默;都挂才提示) |
| 三源全不可达 | 三源都失败 | 弹窗 | "GitHub/CNB/Gitee 都不可达,请检查网络/代理/防火墙" |
| 公司代理/防火墙 | 连接被重置 | 弹窗 | "网络被限制,可能需要配置代理,请联系网络管理员" |
| 网络过慢/超时 | 下载超时 | 重试 N 次 | "网络较慢,重试中…";最终失败→弹窗 |
| wifi 强制门户 | 响应是 HTML 重定向 | 弹窗 | "请先完成 WiFi 登录再运行" |
| 证书错误 | TLS 异常 | 关联系统时间 | "安全证书校验失败,多为系统时间不准" |

### 5.3 磁盘 / 路径
| 场景 | 检测 | 处置 | 用户提示 |
|---|---|---|---|
| 磁盘空间不足 | `shutil.disk_usage` < 阈值 | 弹窗 | "磁盘空间不足,一条龙约需 X GB,请清理 C/D 盘" |
| 路径含中文/特殊字符 | 路径正则 | 弹窗 | "路径含中文或特殊字符,请把整个文件夹移到纯英文路径(如 D:/ZZZ-1D)" |
| 路径过长 | 长度>阈值 | 弹窗 | "路径过长,请移到更浅的目录" |
| 无写权限(如 Program Files) | 临时文件写测试 | 弹窗 | "当前目录无写权限,请移出 Program Files,或以管理员身份运行" |
| 网络盘 / USB / 只读 | 路径所属盘类型 | 弹窗 | "请把一条龙放到本地固态硬盘(SSD)上" |

### 5.4 git 可用性
| 场景 | 检测 | 处置 | 用户提示 |
|---|---|---|---|
| 无 git | `git --version` 失败 | **自动**下 MinGit | (静默,首次约 +8s;小窗显示进度) |
| git 损坏/架构不对 | `--version` 失败 | **自动**下 MinGit | (静默) |
| MinGit 下载失败 | 华为云/淘宝重试均败 | 弹窗 | "无法获取 git,请检查网络;或手动安装 git 后重试" |

### 5.5 项目状态(一条龙)
| 场景 | 检测 | 处置 | 用户提示 |
|---|---|---|---|
| oops.exe 放错位置(找不到 launcher) | 找不到 OneDragon-Launcher.exe | 弹窗 | "请把 oops.exe 放到一条龙根目录(和 OneDragon-Launcher.exe 同级)" |
| 不是 git 仓 / .git 损坏 | git 检查 | 重新克隆(备份后) | "项目 git 数据异常,正在修复/重新下载" |
| origin 指向死 fork / 错源 | remote 检查 | 用专用 remote 从 CNB 强更 | (静默,不碰用户的 origin) |
| 落后 / 分叉 / 本地有改动 | fetch 比对 | **备份后**硬重置到源 HEAD | "已备份你的改动,正在更新到最新"(给回滚方法) |
| 半更新损坏状态 | 工作区/索引异常 | 重新对齐/克隆 | "上次更新未完成,正在修复" |
| 完全没装(无 launcher 也无仓) | 探测 | 全新克隆一条龙 | "未检测到一条龙,正在为你下载安装…" |

### 5.6 启动器 / 运行时
| 场景 | 检测 | 处置 | 用户提示 |
|---|---|---|---|
| launcher 缺失 | 找不到 exe | 触发安装(克隆) | "未安装一条龙,正在下载…" |
| launcher 版本过旧 | 版本比对 | 提示/触发更新 | "启动器版本过旧,建议更新" |
| 一条龙 Python 环境异常 | 查 .venv / env | 提示 | "一条龙 Python 环境异常,详情见日志" |

### 5.7 OOPS 自身
| 场景 | 检测 | 处置 | 用户提示 |
|---|---|---|---|
| OOPS 文件损坏(下载不全) | 自检/解压失败 | 提示重下 | "程序文件损坏,请重新下载 oops.exe" |
| OOPS 有新版本 | self-update 检查 | 倒数自动更新 | (静默) |
| OOPS 自身依赖缺失(不应发生) | 启动即崩 | 兜底崩溃弹窗 | "OOPS 启动失败,见 oops-error.txt" |

## 6. 架构

- **自包含 exe**:PyInstaller 打包 Python + 依赖;`--collect-all tkinter`(弹窗);双击隐藏控制台(`hide_console`)。
- **MinGit 自动获取**:`oops/actions/git_provider.py`(华为云主 / 淘宝备,钉版兜底)。
- **模块分工**:
  - `oops/actions/diagnose.py`(新):快速结构化诊断(系统/磁盘/路径/git/网络三源/项目状态)→ 返回 `Diagnosis` 结果 + 建议的"可用源"与"硬阻塞列表"。
  - `oops/actions/recover.py`(由现 `boot.py`/`auto_fix.py` 收束而来):据诊断,网络通则用专用 remote 从选定源强更(备份 + 硬重置)→ 启动 launcher。
  - `oops/actions/boot_gui.py`:置顶小窗,串起 诊断→恢复→启动,显示进度/诊断/错误;出错留窗。
  - `oops/actions/errors.py`:置顶弹窗 + `oops-error.txt`。
  - `oops/actions/git_ops.py` / `git_provider.py`:git 操作 + 自获取 git。
- **CNB 优先 + 多源兜底**:`oops-cnb` 专用 remote(不碰用户 origin);网络诊断决定实际用哪个源。
- **日志**:`oops.log`(全量运行日志)+ `oops-error.txt`(出错快照,含诊断摘要),与 exe 同目录。

## 7. 明确裁剪(治"乱七八糟")

从双击主线**移除**旧"运行前体检"检测器(它们是"体检",不是"兜底恢复"):
- 硬件类(HDR / 夜间模式 / 分辨率 / GPU)、系统信息收集、PyPI 源探测、米哈游 API、游戏内设置 等。
- 这些降级为 `oops check`(可选,诊断/报告用),**不再是双击默认**。
- 现有 detectors 代码可保留给 `oops check`,但**不进入主流程**。

## 8. 与现状的差距(下一步工作清单,按 SPEC)

**已具备**:
- ✅ 零依赖(exe 自带 Python + MinGit 自动获取)
- ✅ CNB 绕过强更(`oops-cnb` 专用 remote,不碰 origin)
- ✅ 错误置顶弹窗 + `oops-error.txt`(`errors.py`)
- ✅ 置顶小窗 + 隐藏控制台(`boot_gui.py`)

**待做(收束到 SPEC)**:
1. **新增 `diagnose.py`**:系统/磁盘/路径/git/网络(三源)/项目状态 的快速结构化诊断,产出"可用源 + 硬阻塞"。
2. **主流程改诊断驱动**:先诊断 → 硬阻塞直接弹窗 / 否则选定源强更 + 启动。
3. **双击默认切换**:从"旧全文预检"改为这条主线;旧预检降级 `oops check`。
4. **逐条补齐场景矩阵的中文提示文案**(上表"用户提示"列)。
5. **清理/降级** detectors 中与兜底无关的检测项出主线。
6. **`oops-error.txt` 增强**:出错时附诊断摘要(系统/网络/路径/项目概况),让一张照片能定位。

---

## 附:版本里程碑(建议)
- **v0.7.0**:落地 `diagnose.py` + 诊断驱动主流程 + 双击默认切换;旧预检降级 `oops check`。
- **v0.8.0**:补齐场景矩阵剩余项 + `oops-error.txt` 诊断摘要;裁剪主线无关检测。
- 之后:按真实用户反馈迭代场景矩阵。
