# 显示器分辨率检测

## 功能概述

OOPS 现在可以自动检测主显示器分辨率，并验证是否满足游戏脚本的最低要求。

## 为什么需要分辨率检测？

游戏脚本依赖图像识别技术来自动化操作：
- **分辨率过低**会导致UI元素无法正确识别
- **图像模糊**会影响OCR文字识别准确性
- **界面缩放**可能导致坐标偏移

因此，确保显示器分辨率满足最低要求是脚本正常运行的前提。

## 最低要求

- **推荐分辨率**: 1920x1080 (Full HD)
- **宽高比**: 16:9
- **低于此分辨率**: 将显示错误警告

## 检测方法

### Windows
```powershell
Add-Type -AssemblyName System.Windows.Forms
$screen = [System.Windows.Forms.Screen]::PrimaryScreen
$width = $screen.Bounds.Width
$height = $screen.Bounds.Height
```

### Linux
```bash
xrandr --query | grep "connected primary"
```

### macOS
```bash
system_profiler SPDisplaysDataType | grep "Resolution:"
```

## 验证逻辑

```python
def _validate_resolution(resolution: str) -> Dict[str, Any]:
    # 解析分辨率 (例如: "1920 x 1080")
    width, height = parse_resolution(resolution)
    
    # 最低要求
    min_width = 1920
    min_height = 1080
    
    if width < min_width or height < min_height:
        return {
            'valid': False,
            'severity': 'error',
            'message': f'分辨率 {width}x{height} 低于最低要求 {min_width}x{min_height}'
        }
    else:
        return {
            'valid': True,
            'severity': 'info',
            'message': f'分辨率 {width}x{height} 符合要求'
        }
```

## 检测结果示例

### 分辨率符合要求
```
【系统信息】
✅ 通过项:
  基本信息:
    主显示器分辨率: 2560 x 1440
```

### 分辨率过低
```
【系统信息】
❌ 失败项:
  显示设置: 检测到可能影响识别的显示设置
    问题: 主显示器分辨率过低: 1366 x 768
💡 修复建议: 游戏脚本要求最低分辨率 1920x1080，请调整显示器分辨率
```

## 如何调整分辨率

### Windows 10/11
1. 右键桌面 → "显示设置"
2. 滚动到"显示分辨率"
3. 选择 1920x1080 或更高
4. 点击"保留更改"

### Windows 7
1. 右键桌面 → "屏幕分辨率"
2. 在"分辨率"下拉菜单中选择
3. 点击"确定"

### Linux (GNOME)
1. 设置 → 显示器
2. 选择分辨率
3. 点击"应用"

### Linux (KDE)
1. 系统设置 → 显示和监控器
2. 选择分辨率
3. 点击"应用"

### macOS
1. 苹果菜单 → 系统偏好设置
2. 显示器
3. 选择"缩放"或具体分辨率

## 常见问题

### Q: 为什么我的显示器不支持 1920x1080？
A: 可能原因：
- 显示器物理分辨率较低（如老旧显示器）
- 显示驱动未正确安装
- 使用了非标准的显示设备

**解决方案**：
1. 检查显示器规格，确认最大支持分辨率
2. 更新显示驱动程序
3. 如果显示器确实不支持，考虑升级硬件

### Q: 我使用笔记本电脑，分辨率只有 1366x768 怎么办？
A: 建议：
1. 连接外接显示器（支持 1920x1080）
2. 在外接显示器上运行游戏
3. 或者考虑升级到更高分辨率的笔记本

### Q: 我的分辨率是 2560x1440，为什么还提示问题？
A: 如果你的分辨率高于 1920x1080，不应该有问题。请检查：
1. 是否有其他显示设置问题（HDR、夜间模式等）
2. 查看完整的检测报告

### Q: 检测不到分辨率怎么办？
A: 可能原因：
- **Windows**: 缺少 .NET Framework
- **Linux**: 未安装 xrandr (`sudo apt install x11-xserver-utils`)
- **macOS**: 权限不足

## 技术细节

### 分辨率格式
- 标准格式: `1920 x 1080`
- 解析时会去除空格和额外信息
- 支持带偏移量的格式: `1920x1080+0+0`

### 错误处理
- 无法检测: 返回 None，不影响其他检测
- 解析失败: 显示警告，建议手动检查
- 检测超时: 5秒后放弃，继续其他检测

### 性能影响
- 检测时间: < 1秒
- 不会影响系统性能
- 只在启动时检测一次

## 未来计划

### 可能添加的功能
1. **多显示器检测**
   - 检测所有显示器的分辨率
   - 识别游戏运行在哪个显示器

2. **DPI 缩放检测**
   - 检测 Windows DPI 缩放设置
   - 避免界面元素位置偏移

3. **刷新率检测**
   - 检测显示器刷新率
   - 确保游戏流畅运行

4. **色彩深度检测**
   - 检测显示器色彩位深
   - 确保颜色识别准确性

## 相关文档

- [HDR 检测文档](../HDR_DETECTION.md)
- [系统信息检测](../ARCHITECTURE.md#系统信息检测)
- [配置文件说明](../configs/zenless_zone_zero.yaml)
